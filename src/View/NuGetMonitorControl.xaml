<UserControl x:Class="NuGetMonitor.View.NuGetMonitorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:view="clr-namespace:NuGetMonitor.View"
             xmlns:toms="urn:TomsToolbox"
             xmlns:toolkit="clr-namespace:Community.VisualStudio.Toolkit;assembly=Community.VisualStudio.Toolkit"
             toolkit:Themes.UseVsTheme="True"             
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{x:Static view:NugetMonitorViewModel.Instance}">
  <Grid>
    <DockPanel>
      <ToolBar DockPanel.Dock="Top">
        <Button Command="{Binding SoftRefreshCommand}">Soft Refresh</Button>
        <Button Command="{Binding HardRefreshCommand}">Hard Refresh</Button>
        <Button Command="{Binding UpdateSelectedCommand}">Update Selected</Button>
        <Button Command="{Binding ShowNuGetPackageManagerCommand}">NuGet Package Manager</Button>
      </ToolBar>
      <DataGrid ItemsSource="{Binding Packages}"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                toms:MultiSelectorExtensions.SelectionBinding="{Binding SelectedPackages}">
        <DataGrid.RowStyle>
          <Style TargetType="DataGridRow">
            <Setter Property="IsSelected" Value="{Binding IsSelected}" />
          </Style>
        </DataGrid.RowStyle>
        <DataGrid.Columns>
          <DataGridTextColumn Binding="{Binding Identity.Id}" Header="Package" MinWidth="300" />
          <DataGridTextColumn Binding="{Binding Identity.Version}" Header="Installed Version" MinWidth="120">
            <DataGridTextColumn.ElementStyle>
              <Style TargetType="FrameworkElement">
                <Setter Property="HorizontalAlignment" Value="Right" />
              </Style>
            </DataGridTextColumn.ElementStyle>
          </DataGridTextColumn>
          <DataGridTemplateColumn Header="Available Versions" MinWidth="120">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <Grid>
                  <Grid.Resources>
                    <Style TargetType="ComboBox" BasedOn="{StaticResource {x:Static ToolBar.ComboBoxStyleKey}}">
                      <Style.Triggers>
                        <Trigger Property="IsDropDownOpen" Value="False">
                          <Setter Property="Background" Value="Transparent" />
                          <Setter Property="BorderBrush" Value="Transparent" />
                        </Trigger>
                      </Style.Triggers>
                    </Style>
                  </Grid.Resources>
                  <ComboBox ItemsSource="{Binding Package.Versions}"
                            SelectedItem="{Binding SelectedVersion}"
                            HorizontalAlignment="Right" />
                  <Grid x:Name="LoadingItemText" Visibility="Collapsed">
                    <ProgressBar IsIndeterminate="True" />
                    <TextBlock Text="Loading..." VerticalAlignment="Center" HorizontalAlignment="Center" />
                  </Grid>
                </Grid>
                <DataTemplate.Triggers>
                  <DataTrigger Binding="{Binding Package}" Value="{x:Null}">
                    <Setter TargetName="LoadingItemText" Property="Visibility" Value="Visible" />
                  </DataTrigger>
                </DataTemplate.Triggers>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
          <DataGridTemplateColumn SortMemberPath="IsUpdateAvailable" Width="36">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <Button Content="Update this" MinWidth="16"
                        IsEnabled="{Binding IsUpdateAvailable, Mode=OneWay}"
                        Command="{Binding Update}">
                  <Button.Template>
                    <ControlTemplate TargetType="{x:Type Button}">
                      <Grid Width="16" Height="16" Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Ellipse x:Name="Ellipse" Width="16" Height="16" Stroke="Black" StrokeThickness="1" />
                        <Path Data="M8,4 L8,13 M5,8 L8,4 L11,8" Stroke="Black" StrokeThickness="1"/>
                      </Grid>
                      <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                          <Setter Property="Visibility" Value="Hidden" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                          <Setter TargetName="Ellipse" Property="Fill" Value="Gray" />
                        </Trigger>
                      </ControlTemplate.Triggers>
                    </ControlTemplate>
                  </Button.Template>
                </Button>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
        </DataGrid.Columns>
      </DataGrid>
    </DockPanel>
    <toms:LoadingIndicator IsActive="{Binding IsLoading}" Header="Loading..." />
  </Grid>
</UserControl>